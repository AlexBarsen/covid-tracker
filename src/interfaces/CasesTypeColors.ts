export interface CasesTypeColors {
  cases: {
    hex?: string;
    rgb: string;
    half_op: string;
    multiplier: number;
  };
  recovered: {
    hex: string;
    rgb: string;
    half_op: string;
    multiplier: number;
  };
  deaths: {
    hex: string;
    rgb: string;
    half_op: string;
    multiplier: number;
  };
}
